# R2 全向轮底盘完整定义

> 单一事实来源。坐标系、CAN 映射、运动学、物理参数全部在此。

---

## 一、坐标系约定

### 车体坐标系

```
         前 (vx⁺)
          ↑
          |
    左 ←──┼──→ 右 (vy⁺)
     (vy⁻) |
          ↓
         后 (vx⁻)

   ω⁺ = 从上方看逆时针旋转
```

| 轴 | 正方向 | 说明 |
|:---|:-------|:------|
| vx | **前** | 车头指向为 + |
| vy | **左** | 车体左侧为 + |
| ω | **逆时针** | 从上方看，车体逆时针转为 + |

---

## 二、CAN ID 与轮子对应

### 物理布局

电机在车体上按**逆时针**排列（从上方看）：

```
              前 (vx⁺)

         FL(0x123)    FR(0x126)
              ╲   ↑   ╱
               ╲  ↑  ╱
           左 ←───中───→ 右
               ╱     ╲
              ╱       ╲
         RL(0x124)    RR(0x125)

              后 (vx⁻)

   逆时针绕一圈: 0x123(FL) → 0x124(RL) → 0x125(RR) → 0x126(FR)
```

### 映射表

| 轮子 | 代号 | CAN 命令 ID | CAN 状态 ID | MCLM Group |
|:----:|:----:|:----------:|:-----------:|:-----------|
| 左前 | FL | **0x123** | 0x323 | GROUP=2 |
| 右前 | FR | **0x126** | 0x326 | GROUP=1 |
| 左后 | RL | **0x124** | 0x324 | GROUP=2 |
| 右后 | RR | **0x125** | 0x325 | GROUP=1 |

### 方向约定

**电机正转** = 收到正速度命令时**逆时针旋转**（从电机输出轴端看）。

**注意**：由于运动学公式坐标系与车体坐标系存在 90° 旋转（经 `calibrate_direction.py` 8 组测试校准），单个电机的旋转方向与车体运动方向之间没有直接的简单对应关系。车体运动是所有 4 个轮子合成力的结果。

运动学变换关系:
```
# _cmd_callback 中的坐标变换（实测校准）:
kin_vx = -user_vy     # 用户的左右 → 公式的前后
kin_vy =  user_vx     # 用户的前后 → 公式的左右
```

### 实测轮速组合与车体运动对照表

| 测试 | FL(0x123) | FR(0x126) | RL(0x124) | RR(0x125) | 车体运动 |
|:----:|:---------:|:---------:|:---------:|:---------:|:---------|
| P2 | **+20** CCW | **-20** CW | **+20** CCW | **-20** CW | **前进** |
| P4 | **-20** CW | **-20** CW | **+20** CCW | **+20** CCW | **左移** |
| P1 | **+20** CCW | **+20** CCW | **-20** CW | **-20** CW | **右移** |
| P3 | **-20** CW | **+20** CCW | **-20** CW | **+20** CCW | **后退** |
| P5 | -20 CW | -20 CW | -20 CW | -20 CW | 左旋 |
| P6 | +20 CCW | +20 CCW | +20 CCW | +20 CCW | 右旋 |

---

## 三、不同运动方向的轮速分布

### 前进 vx=0.3

| 轮 | ID | 速度 | 旋向 | 车体效果 |
|:--:|:--:|:----:|:----:|:---------|
| FL | 0x123 | **+20** | 逆时针 | 推车前进 ✅ |
| FR | 0x126 | **+20** | 逆时针 | 推车前进 ✅ |
| RL | 0x124 | **-20** | 顺时针 | 推车前进 ✅ |
| RR | 0x125 | **-20** | 顺时针 | 推车前进 ✅ |

前轮同向(+)、后轮同向(-)。**所有 4 轮都在贡献前进推力。**

### 后退 vx=-0.3

| 轮 | 速度 |
|:--:|:----:|
| FL | -20 |
| FR | -20 |
| RL | +20 |
| RR | +20 |

前后轮对调方向。

### 左移 vy=0.2

| 轮 | 速度 | 车体效果 |
|:--:|:----:|:---------|
| FL | +13 | 左移 ✅ |
| FR | -13 | 左移 ✅ |
| RL | +13 | 左移 ✅ |
| RR | -13 | 左移 ✅ |

左侧轮同向(+)、右侧轮同向(-)。

### 自转 ω=0.5

| 轮 | 速度 |
|:--:|:----:|
| FL | -16 |
| FR | -16 |
| RL | -16 |
| RR | -16 |

4 轮同向顺时针。

### 45° 右前 (vx=0.3, vy=0.3)

| 轮 | 速度 | 说明 |
|:--:|:----:|:------|
| FL | **+40** | 承担推力 |
| FR | **0** | 停转（被动拖行） |
| RL | **0** | 停转（被动拖行） |
| RR | **-40** | 承担推力 |

> ⚠ 45° 时两个轮子停转是**全向轮运动学的正常现象**。如果车体抖动，通常是辊子摩擦力太大或 `R` 值不精确。

---

## 四、CAN 协议

### 命令帧（NUC → MCLM，8 字节）

| Byte | 字段 | 类型 | 值 | 说明 |
|:----:|:-----|:----:|:---|:------|
| 0 | cmd | uint8 | `0x11` | SET_SPEED |
| | | | `0x08` | STOP |
| 1 | speed | int8 | -100~+100 | 速度值（正=逆时针） |
| 2~7 | — | uint8 | 0x00 | 保留 |

### 状态帧（MCLM → NUC，每 50ms 上报，8 字节）

| Byte | 字段 | 类型 | 范围 | 说明 |
|:----:|:-----|:----:|:----:|:------|
| 0~1 | current_speed | int16 LE | -100~+100 | 当前实际速度 |
| 2~3 | accumulated_ticks | uint16 LE | 0~65535 | 编码器累计脉冲 |
| 4~5 | pwm_output | int16 LE | 0~7200 | PWM 输出 |
| 6 | target_speed | int8 | -100~+100 | 目标速度 |
| 7 | flags | uint8 | — | bit0=STALL, bit1=SATURATED |

---

## 五、运动学公式

### 逆解（车体速度 → 4 轮速度）

```python
INV_SQRT2 = 1/√2 ≈ 0.7071
R = 0.33  # 半对角线长 (m)

# 输出为 m/s，乘以 speed_scale 取整得 CAN 逻辑速度
FL = ( vx + vy) * INV_SQRT2 - R * ω    # → 0x123
FR = ( vx - vy) * INV_SQRT2 - R * ω    # → 0x126
RL = (-vx + vy) * INV_SQRT2 - R * ω    # → 0x124
RR = (-vx - vy) * INV_SQRT2 - R * ω    # → 0x125
```

### 正解（4 轮速度 → 车体速度）

```python
vx    = (FL + FR - RL - RR) / (4 * INV_SQRT2)
vy    = (FL - FR + RL - RR) / (4 * INV_SQRT2)
omega = -(FL + FR + RL + RR) / (4 * R)
```

---

## 六、物理参数

### `r2_params.yaml`

```yaml
/**:
  ros__parameters:
    wheel_half_diagonal: 0.33    # R (m)
    ticks_per_rev: 4241           # ticks/圈
    wheel_diameter: 0.152         # 轮径 (m)
    speed_scale: 94.5             # CAN 速度→m/s 系数
    cmd_timeout: 0.5              # 超时(s)
    odom_publish_rate: 50.0       # 里程计频率
    can_channel: 'can0'
    max_vx: 0.5                   # m/s
    max_vy: 0.3                   # m/s
    max_omega: 0.8                # rad/s
```

### speed_scale 推导

```
实测: speed=10 → 940 ticks/s
     → 940/4241 × π × 0.152 = 0.1058 m/s
     → speed_scale = 10 / 0.1058 ≈ 94.5
```

### 里程计刻度

```python
m_per_tick = π × 0.152 / 4241 = 0.000113 m = 0.113 mm
```

---

## 七、ROS2 接口

| 话题 | 类型 | 方向 | 说明 |
|:-----|:-----|:----:|:------|
| `/cmd_vel` | Twist | 订阅 | 车体速度指令 |
| `/odom_wheels` | Odometry | 发布 | 纯轮速里程计 |
| `/tf` | TFMessage | 发布 | odom → base_link |

TF 树：`odom` → `base_link`

---

## 八、启动

```bash
# CAN 总线
sudo ip link set can0 up type can bitrate 1000000
candump can0

# 启动节点
source ~/Lin_workspace/r2_integration/r2_bringup/install/setup.bash
ros2 launch r2_bringup chassis.launch.py

# 键盘控制
ros2 run teleop_twist_keyboard teleop_twist_keyboard
```

---

## 九、常见问题

### 前进时哪几个轮子转？
**全部 4 轮都转**。前轮(0x123+0x126)逆时针，后轮(0x124+0x125)顺时针，都在推车前进。

### 45° 方向为什么抖？
两个轮子停转是正常的运动学特性。降低速度或检查 R 值。

### 某个轮子不转？
检查 CAN ID 是否硬编码错了（多用 `R2_MOTOR_IDS` 查表）。用 `--test` 模式单独测试该轮。

### ticks 回绕？
uint16 溢出在 `decode_status_frame()` 中已自动处理。

---

## 十、版本历史

| 版本 | 日期 | 变更 |
|:----:|:----:|:------|
| v2.0 | 2026-07-29 | **修正 CAN ID→轮子映射**：逆时针 0x123→0x124→0x125→0x126 |
| v1.0 | 2026-07-29 | 初始定义 |
